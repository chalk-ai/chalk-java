package ai.chalk.internal.config.models;

import java.util.HashMap;

public class ProjectTokens {

    private HashMap<String, ProjectToken> tokens;

    // getters and setters
    public HashMap<String, ProjectToken> getTokens() { return tokens; }
    public void setTokens(HashMap<String, ProjectToken> tokens) { this.tokens = tokens; }
}
